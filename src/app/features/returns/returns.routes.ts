// src/app/features/returns/returns.routes.ts
import { Routes } from '@angular/router';
import { ReturnsComponent } from './returns';

export default [
  { path: '', component: ReturnsComponent, title: 'Devoluciones' },
] as Routes;
