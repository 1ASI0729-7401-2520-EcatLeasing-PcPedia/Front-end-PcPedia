// src/app/features/catalog/catalog.routes.ts
import { Routes } from '@angular/router';
import { CatalogComponent } from './catalog';

export default [
  { path: '', component: CatalogComponent, title: 'Catálogo' },
] as Routes;
